<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @session_login.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @session_login.content %>
</p>

<%= link_to 'Edit', edit_session_login_path(@session_login) %> |
<%= link_to 'Back', session_logins_path %>
